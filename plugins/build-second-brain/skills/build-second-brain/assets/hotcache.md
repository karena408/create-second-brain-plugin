---
title: Hot Cache
source:
author:
published:
created:
description: 5–8 most recently touched wiki pages (sliding window). Trim the bottom on every prepend.
tags: [hotcache, system, meta]
type: synthesis
---

# Hot Cache

Sliding window of the **5–8 most recently touched pages**, body ≤500 chars. Format: `- [[Page-Name]] — short reason (op + date).` Prepend on write or substantial read; trim the bottom. Chronology lives in `log.md`, not here.

---

*(empty — populated as the wiki grows)*
